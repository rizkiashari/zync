# Salin ke .env.prod — npm run build:prod (mode prod).
# Build biasa / Capacitor: pakai .env.production (npm run build / build:mobile).
VITE_API_URL=http://YOUR_VPS_IP:8080

# VITE_MIDTRANS_CLIENT_KEY=
# VITE_MIDTRANS_IS_PRODUCTION=false
